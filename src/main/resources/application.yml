
spring.h2.console:
  enabled: true
  path: /h2-console

security:
  user:
    name: user
    password: test

  oauth2:

    client:
      client-id: foo
      client-secret: bar
    authorization:
      token-key-access: isAuthenticated()


logging.level:
  org.springframework:
    security: debug
    boot: debug
